Steps to Run:

The project needs HDFS & Spark 1.6.0 to run. We implemented this on NYU HPC & Cloudera.

1. Put the files Preprocessed_data.csv & Preprocessed_datC.csv on hdfs.

2. Run Command: spark-shell -i FileName.scala